version: "3.9"

services:

  backend:
    build: ./backend
    ports:
      - "7777:7777"
    volumes:
      - ./sandbox/datasets/clean:/app/dataset

  ml-api:
    build: ./ml-api
    ports:
      - "5000:5000"
    volumes:
      - ./sandbox/datasets/clean:/app/dataset